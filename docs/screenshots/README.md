# Screenshots

Drop terminal screenshots here, then uncomment the screenshot block near the top of the root `README.md`.

Expected files:

| File | Shows |
|---|---|
| `distill.png` | `teach2claude distill` — recurring corrections ranked by repetition count |
| `context.png` | `teach2claude context` — startup context-window audit |
| `search.png` | `teach2claude search "..."` — full-text history search |

Tips: dark terminal theme, ~110 columns wide, crop to the command + output only.
